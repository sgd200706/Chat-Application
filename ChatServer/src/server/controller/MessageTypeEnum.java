package server.controller;

/********* the message type much exactly matches the following*************/

public enum MessageTypeEnum
{
	login, 
	signup, 
	newuser,
	newroom, 
	message, 
	selectroom,
	useroffline
}